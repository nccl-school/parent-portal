meta {
  name: Create a folder (owner=school)
  type: http
  seq: 9
}

post {
  url: {{API_DOMAIN}}/api/resource/folder
  body: json
  auth: inherit
}

body:json {
  {
    "name": "folder 2",
    "slug": "folder-2",
    "ownership": {
      "level": "school"
    }
  }
}
