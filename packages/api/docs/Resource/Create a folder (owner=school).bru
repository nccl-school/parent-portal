meta {
  name: Create a folder (owner=school)
  type: http
  seq: 9
}

post {
  url: {{API_DOMAIN}}/api/resource/folder
  body: json
  auth: inherit
}

body:json {
  {
    "name": "folder 4",
    "slug": "folder-4",
    "owner": "school"
  }
}
