meta {
  name: Create a folder (owner=user)
  type: http
  seq: 7
}

post {
  url: {{API_DOMAIN}}/api/resource/folder
  body: json
  auth: inherit
}

body:json {
  {
    "name": "folder 4",
    "slug": "folder-4",
    "owner": "user",
    "userId": "test"
  }
}
