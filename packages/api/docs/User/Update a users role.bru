meta {
  name: Update a users role
  type: http
  seq: 3
}

put {
  url: {{API_DOMAIN}}/api/user/:user_id/role
  body: json
  auth: inherit
}

params:path {
  user_id: 
}

body:json {
  {
    "role": "USER"
  }
}
