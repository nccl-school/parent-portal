meta {
  name: Update current user's profile
  type: http
  seq: 5
}

patch {
  url: {{API_DOMAIN}}/api/user/my-profile
  body: json
  auth: inherit
}

body:json {
  {
    "lastName": "Kent"
  }
}

settings {
  encodeUrl: true
}
